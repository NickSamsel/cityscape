version: 2

models:
  - name: stg_mlb__games
    description: "MLB games staged from raw landing tables"
    columns:
      - name: game_id
        tests: [not_null]
  - name: stg_mlb__teams
    description: "MLB teams staged from raw landing tables"
    columns:
      - name: team_id
        tests: [not_null]
