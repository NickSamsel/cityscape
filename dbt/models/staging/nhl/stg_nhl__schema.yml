version: 2

models:
  - name: stg_nhl__games
    description: "NHL games staged from raw landing tables"
    columns:
      - name: game_id
        tests: [not_null]
  - name: stg_nhl__teams
    description: "NHL teams staged from raw landing tables"
    columns:
      - name: team_id
        tests: [not_null]
