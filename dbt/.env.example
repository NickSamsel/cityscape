# Copy to dbt/.env and fill in values.
# These are referenced by profiles.yml (which you usually keep OUT of git).

DBT_HOST=localhost
DBT_PORT=5432
DBT_USER=postgres
DBT_PASSWORD=postgres
DBT_DBNAME=cityscape
DBT_SCHEMA=analytics
