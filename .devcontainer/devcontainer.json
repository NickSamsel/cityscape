{
  "name": "cityscape",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "dev",
  "workspaceFolder": "/workspaces/cityscape",
  "shutdownAction": "stopCompose",
  "overrideCommand": false,
  "postCreateCommand": "until [ -f /tmp/gcp-key.json ]; do echo 'Waiting for GCP key...'; sleep 1; done && echo 'GCP key ready.'",
  "remoteUser": "root",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbt-labs.dbt-bitbucket-adapter",
        "Anthropic.claude-code"
      ]
    }
  }
}